
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>

    <style>
        div { margin: 0; padding: 0; }
        .wrapper { width: 70%; margin: auto; }
        .nav { background: red; }
        .nav div { width: calc(25% - 2px); height: 50px; background: #eee; float: left; text-align: center; line-height: 50px; color: black; border: 1px solid #ccc; }
        .content { width: 100%; background: green; padding-top: 70px; }
        .content .list { width: 90%; margin: auto; background: #ccc; }
        .content .list .item { width: 100%; height: 40px; margin-bottom: 5px; background: aqua; border-bottom: 2px solid black; }
        .content .list .item div { width: 20%; }
        .content .list .item .rank { height: 100%; background: tomato; text-align: center; line-height: 40px; }
        .content .list .item .title { height: 100%; background: pink; text-align: center; line-height: 40px; }
        .content .list .item .musition { height: 100%; background: white; text-align: center; line-height: 40px; }
        .content .list .item .album { height: 100%; background: yellow; text-align: center; line-height: 40px; }
        .content .list .item .date { height: 100%; background: yellow; text-align: center; line-height: 40px; }
    </style>
</head>
<body>
    <div class= "wrapper">
        <div class= "nav">
            <div>HOME</div>
            <div>MELON</div>
            <div>KKUJEO</div>
            <div>BACK</div>
        </div>
        <div class= "content">
            <div class="list">
                <% for(var i = 0; i < melon_list.length; i++) { %>
                    <div class="item">
                        <div class="rank"><%= melon_list[i].ranking%></div>
                        <div class="title"><%= melon_list[i].title%></div>
                        <div class="musition"><%= melon_list[i].musition%></div>
                        <div class="album"><%= melon_list[i].album%></div>
                        <div class="date"><%= moment(melon_list[i].regist_dt).format("YYYY-MM-DD") %></div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>